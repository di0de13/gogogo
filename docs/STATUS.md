# 当前状态

更新时间：2026-09-02

## 当前定位

以 LiveGrow 直播营收增长平台为主项目，建立“AI/算法背景 → Go 后端工程 → 分布式营收系统”的面试叙事。

## 已完成

- 检查工作区：初始为空目录；
- 确认可以在工作区内操作 Markdown、TXT、JSON、YAML、Go 源码和测试文件；
- 创建 `docs`、`project/livegrow`、`records`；
- 创建总体计划和最低单元标准；
- 探测本地 Go 版本：go1.27.0 darwin/arm64；
- 探测本地 Git 版本：2.50.1 Apple Git-155；
- 初始化 Go module 和最小可运行程序；
- 完成第一次测试验证：`GOCACHE=/private/tmp/livegrow-gocache go test ./...` 通过，程序输出 `livegrow 0.1.0`；
- 记录沙箱注意事项：Go 默认缓存目录不可写，后续统一使用工作区外的允许临时缓存路径。
- 完成 `2.1.1`：明确用户角色、直播间/订单/增长活动场景、核心流程、数据一致性等级、成功指标和非目标范围；
- 产出业务基线文档：`docs/01-BUSINESS-REQUIREMENTS.md`。
- 完成 `2.1.2`：细化订单、支付、礼物发放和 Outbox 状态；定义幂等、事件、重试、死信、对账和测试用例；
- 产出订单领域文档：`docs/02-ORDER-DOMAIN.md`。
- 完成 `2.1.3`：定义活动生命周期、用户稳定分流、预算控制、灰度护栏、回滚语义、营收指标闭环和 AI Copilot 边界；
- 产出增长实验文档：`docs/03-GROWTH-EXPERIMENT.md`。
- 完成 `2.2.1`：明确 Room、Order、Growth 的职责、数据所有权、API、事件契约、依赖方向和故障隔离；
- 产出服务边界文档：`docs/04-SERVICE-BOUNDARIES.md`。
- 完成 `2.2.2`：统一订单、活动和 Outbox 状态机，定义非法迁移、幂等、并发版本和审计规则；
- 产出状态机文档：`docs/05-STATE-MACHINES.md`；
- 完成第 2 阶段复盘，复盘规范见 `docs/REVIEW-PROTOCOL.md`，复盘记录见 `records/reviews/PHASE-02-DOMAIN-MODELING.md`。

## 当前工作单元

`2.2.2 关键状态机`：已完成。

## 下一单元

`2.3.1 30 秒、2 分钟、5 分钟项目讲解`：将第 2 阶段设计压缩成面试可用叙事，并开始转化为 Go 代码任务。

## 当前风险

- 项目范围容易膨胀，必须优先保证订单主链路；
- 没有真实生产数据，所有性能数字必须来自本地实验并标注环境；
- AI Copilot 只能作为候选配置生成器，不能直接变更核心账务状态；
- 不应过早引入过多中间件，避免“会配置但讲不透”。
